# Development Handoff — 2026-08-01

## Session start

- Objective: Stabilize the current web-app baseline, then continue the responsive public landing-page hero.
- Starting branch: `main` (tracking `origin/main`)
- Starting HEAD: `c926cb28f8f993a119e9ad65c5aa790d0f9ade0d` (`More landing page progress`)
- No earlier daily handoff was present. The design handoff remains the primary product/visual contract, but repository state has moved beyond its original no-router implementation note: TanStack Router is installed and active.

### Pre-existing changes

- Web work in progress: `apps/web/package.json`, `apps/web/package-lock.json`, `apps/web/src/index.css`, and `apps/web/src/routes/(pre-auth)/landing.tsx`.
- ADR edits: `docs/adr/0001-web-framework.md`, `docs/adr/0002-css-framework.md`, and `docs/adr/000X-react-routing-tool.md`.
- Untracked design/reference assets under `docs/design/assets/` and the semantic-token implementation plan under `docs/superpowers/plans/`.
- Untracked `.superpowers/brainstorm/` output includes HTML drafts, an image, and transient server state (`server.pid`, `server-stopped`); treat this as questionable tooling residue unless deliberately retained.

### Review findings

- `npm.cmd run build`: passed. Vite warned that `(pre-auth)/landing.tsx` looks like a route file but exports no `Route`; it is currently imported as a component by the index route.
- `npm.cmd run lint`: failed before linting. `eslint.config.js` uses unsupported `globalIgnores` inside a config object; the generated-route exclusion needs a valid flat-config form.
- The landing-page diff is incomplete: it contains an empty bordered block, and `lucide-react` is newly installed but not yet imported from application source.
- The routing ADR still contains template placeholders and proofreading issues, and remains `Proposed` although TanStack Router is already in use.
- `git diff --check`: no whitespace errors. Git emitted LF-to-CRLF conversion warnings for package/CSS files.
- No files were staged and no commit was created because verification is not clean and the changed/untracked scope is mixed.

### Recommended first action

Correct the ESLint flat-config ignore declaration in `apps/web/eslint.config.js`, then rerun `npm.cmd run lint` and `npm.cmd run build`. Once the baseline is green, finish the empty landing-page value/trust block and verify the hero at desktop and mobile widths before deciding which assets and ADR edits belong in the next commit.
